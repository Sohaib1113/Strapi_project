[build]
  base = "Frontend"
  command = "npm install --legacy-peer-deps && npm run build"
  publish = ".netlify/output"  # relative to base, plugin will create it

[[plugins]]
  package = "@netlify/plugin-nextjs"