[build]
  base = "Frontend"
  command = "npm install --legacy-peer-deps && npm run build"
  publish = ".netlify/output"  # relative to base, not Frontend/.netlify/output

[[plugins]]
  package = "@netlify/plugin-nextjs"